# Focused review / stronger-model handoff

Status: no implementation blocker reported yet.

## Fill only for a real review or blocker

Issue / invariant at risk:
Active phase and actual model:
Relevant code paths and lines:
Minimal reproduction:
Exact failing test and command:
Observed result:
Expected result:
Attempts and evidence (maximum two failed speculative cycles):
Smallest unresolved question:
Permitted edit scope:
Regression tests required:
Known compatibility/security constraints:

Ask for diagnosis and a bounded patch plan first. Do not ask for a full repository rewrite. Do not resolve failures by deleting tests, relaxing authentication, exposing answers, weakening transaction guarantees, or making clients authoritative.

For a release audit, specify one area and its tests/evidence. A generic approval without inspecting the relevant implementation does not satisfy a release gate.
