# Verified implementation environment

Not yet inspected for this new project. This planning pack does not certify a Node, Java, Android SDK, Electron, Capacitor, or Colyseus version.

Phase 1 records compatible current Node/npm/tool versions and the committed dependency lockfile. Phase 2 records Windows, JDK, Android SDK/build tools, devices, artifact paths, and dedicated-runner setup. Later phases record database/auth/hosting configurations without exposing credentials.

Required fields: tool/version; check command; observed output; supported target; prerequisite status; environment variables by NAME only; artifact directory; test runner location; date/commit tested.

Do not install/change global tools, edit global Codex settings, or start paid infrastructure merely to fill this document. Use owner-approved project-local setup and report blockers.
